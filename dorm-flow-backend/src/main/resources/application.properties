# ==========================================
# PostgreSQL Database Connection Settings
# ==========================================
spring.config.import=optional:file:.env[.properties]

spring.datasource.url=jdbc:postgresql://pg-3800d626-venuriperera419-3ab2.j.aivencloud.com:14812/defaultdb?sslmode=require
spring.datasource.username=avnadmin

# Read the password securely from the local .env file configuration
spring.datasource.password=${AIVEN_DB_PASSWORD}

# ==========================================
# JPA / Hibernate Configurations
# ==========================================
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
